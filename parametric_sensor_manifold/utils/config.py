# Viewer and debug settings
VIEWER_PORT = 3939

# OCP viewer defaults
VIEWER_DEFAULTS = {
    "reset_camera": "keep",  # can be "keep" or "reset"
    "ortho": True,
    "black_edges": True,
    "colormap": {
        "colormap": "rgb",
        "alpha": 1,
        "seed_value": "vscode",
    },
}
